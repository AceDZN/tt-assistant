{
  "$defs": {
    "ImageAnswer": {
      "properties": {
        "answer": {
          "description": "an answer",
          "maxLength": 40,
          "title": "Answer",
          "type": "string"
        },
        "image_prompt": {
          "description": "the image_prompt returned from the `create_images` call",
          "maxLength": 40,
          "minLength": 10,
          "title": "Answer Image Search",
          "type": "string"
        },
        "image_url": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "description": "image url created by `create_images` function",
          "title": "Answer Image Url"
        },
        "image_id": {
          "type": "string",
          "description": "the image_id returned from the `create_images` call",
          "title": "Image Id"
        },
        "correct": {
          "title": "Correct",
          "type": "boolean"
        }
      },
      "required": ["answer", "image_prompt", "image_url", "image_id", "correct"],
      "title": "ImageAnswer",
      "type": "object"
    }
  },
  "properties": {
    "image_prompt": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "description": "the image_prompt returned from the `create_images` call",
      "title": "Image Search"
    },
    "image_url": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],

      "description": "the image_url returned from the `create_images` call, only the original image_url response from the `create_images` call is allowed, identified by {slide index}",
      "title": "Image Url"
    },
    "image_id": {
      "type": "string",
      "description": "the image_id returned from the `create_images` call",
      "title": "Image Id"
    },
    "activity_type": {
      "const": "QuizWithImageAnswers",
      "title": "Activity Type"
    },
    "question": {
      "description": "ask a question about the subject, the player will answer by clicking on one of 3 image options. example: question-which image represents france? answer-image #2 shows eifel tower.",
      "maxLength": 30,
      "title": "Question",
      "type": "string"
    },
    "right_answer_feedback": {
      "description": "feedback to give when the player answers correctly.",
      "maxLength": 100,
      "title": "Right Answer Feedback",
      "type": "string"
    },
    "hint": {
      "description": "hint how to answer the question correctly.",
      "maxLength": 100,
      "title": "Hint",
      "type": "string"
    },
    "image_type": {
      "enum": ["AnimatedGifHttps", "Clipart", "Photo"],
      "title": "Image Type",
      "type": "string"
    },
    "answers": {
      "description": "different unique image search answers to the question, only one is the correct answer",
      "items": {
        "$ref": "#/$defs/ImageAnswer"
      },
      "maxItems": 3,
      "minItems": 2,
      "title": "Answers",
      "type": "array"
    },
    "description": {
      "const": "a text question on the subject; player click on one of three image options.",
      "title": "Description"
    }
  },
  "required": [
    "image_prompt",
    "image_url",
    "image_id",
    "activity_type",
    "question",
    "right_answer_feedback",
    "hint",
    "image_type",
    "answers",
    "description"
  ],
  "title": "QuizWithImageAnswers",
  "type": "object"
}

